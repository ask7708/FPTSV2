package model;

public class ReadTransaction {
	
	//Transaction Format
	//Transaction id, Ticker (Equity or Account), Name of Equity/Account
	//CurrentValue, #Shares (amount for account), Routing num, bankName
	
	public Transaction readTransaction(String[] temp){
		if(temp[1] == "!bank"){
			
		}
		else{
			
		}
		return null;
	}
}
